import { Text } from 'react-native';import { Button,Card,Heading,Notice,Screen,text } from '@/components/ui';import { useSession } from '@/providers/SessionProvider';
export default function Account(){const {session,signOut}=useSession();const p=session?.user.profile;return <Screen><Heading eyebrow="Account" title="Profile & settings" body="Review the contact information currently held by the practice."/><Card><Text style={text.title}>{session?.user.name}</Text><Text style={text.body}>{session?.user.email}</Text><Text style={text.body}>{session?.user.phone??'No phone number recorded'}</Text></Card><Card><Text style={text.title}>Communication preference</Text><Text style={text.body}>{p?.preferred_communication??'Not recorded'}</Text><Text style={text.muted}>Appointment reminders are delivered through the channels configured by the practice. Native push alerts are not active yet.</Text></Card><Notice title="Need to make a change?" body="For safety, contact the practice to correct personal or emergency-contact information until secure profile editing is enabled."/><Button title="Sign out of this device" variant="danger" onPress={()=>void signOut()}/></Screen>}
