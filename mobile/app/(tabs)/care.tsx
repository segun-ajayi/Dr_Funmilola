import { Link } from 'expo-router';import { StyleSheet,Text,View } from 'react-native';import { Card,Heading,Screen,text } from '@/components/ui';import { colors,spacing } from '@/theme';
const items=[['Appointments','Upcoming and past visits','/appointments'],['Consultations','Waiting-room status','/consultations'],['Messages','Secure practice conversations','/messages'],['Documents','Files shared with your care team','/documents'],['Notifications','Important account and care updates','/notifications']] as const;
export default function Care(){return <Screen><Heading eyebrow="Your care" title="Care centre" body="Open the area you need. Your information stays behind your secure session."/>{items.map(([title,body,href])=><Link key={href} href={href} asChild><Card label={`Open ${title}`}><View style={s.row}><View style={s.copy}><Text style={text.title}>{title}</Text><Text style={text.body}>{body}</Text></View><Text style={s.arrow}>›</Text></View></Card></Link>)}</Screen>}
const s=StyleSheet.create({row:{flexDirection:'row',alignItems:'center',gap:spacing.sm},copy:{flex:1},arrow:{fontSize:30,color:colors.green}});
