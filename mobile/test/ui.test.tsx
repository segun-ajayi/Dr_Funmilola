import { fireEvent,render } from '@testing-library/react-native';import { Button,Empty,Heading } from '@/components/ui';
it('provides accessible headings, empty states and controls',async()=>{const onPress=jest.fn();const view=await render(<><Heading title="Appointments"/><Empty title="No appointments yet" body="Your visits will appear here."/><Button title="Try again" onPress={onPress}/></>);expect(view.getByRole('header',{name:'Appointments'})).toBeTruthy();expect(view.getByText('No appointments yet')).toBeTruthy();fireEvent.press(view.getByRole('button',{name:'Try again'}));expect(onPress).toHaveBeenCalledTimes(1);});
