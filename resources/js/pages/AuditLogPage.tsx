import { useQuery } from '@tanstack/react-query';import { ArrowLeft,History,Search } from 'lucide-react';import { useState } from 'react';import { Link } from 'react-router-dom';import { api } from '../api';
export default function AuditLogPage(){const[action,setAction]=useState('');const q=useQuery({queryKey:['audit-logs',action],queryFn:async()=>(await api.get('/cms/audit-logs',{params:{action:action||undefined}})).data,retry:false});if(q.isError)return <section className="portal-guest"><h1>Power Admin access required.</h1><Link className="btn btn-primary" to="/sign-in">Sign in</Link></section>;return <main className="staff-main management-page"><header><div><span className="eyebrow">Security & accountability</span><h1>Audit log</h1><p>Review administrative changes from the last 30 days.</p></div><Link className="btn btn-outline-primary" to="/staff"><ArrowLeft/> Dashboard</Link></header><section className="staff-card"><label className="audit-search"><Search/><span>Filter by action</span><input className="form-control" value={action} onChange={e=>setAction(e.target.value)} placeholder="For example: cms."/></label>{q.isLoading?<p>Loading activity…</p>:q.data?.data?.length?q.data.data.map((log:any)=><article className="audit-row" key={log.id}><History/><div><h2>{log.action}</h2><p>{log.actor?.name??'System'} · {log.subject_type} #{log.subject_id??'—'}</p></div><time>{new Date(log.created_at).toLocaleString('en-NG')}</time></article>):<div className="staff-empty"><History/><p>No matching activity in this period.</p></div>}</section></main>}
