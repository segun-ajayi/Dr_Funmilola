import { render,screen } from '@testing-library/react';import { QueryClient,QueryClientProvider } from '@tanstack/react-query';import { MemoryRouter } from 'react-router-dom';import { vi } from 'vitest';import EducationPage from './EducationPage';
const get=vi.fn();vi.mock('../api',()=>({api:{get:(...args:unknown[])=>get(...args)}}));
test('labels reviewed educational content and keeps the empty state useful',async()=>{get.mockResolvedValueOnce({data:{data:[],total:0}});render(<QueryClientProvider client={new QueryClient()}><MemoryRouter><EducationPage/></MemoryRouter></QueryClientProvider>);expect(screen.getByRole('heading',{name:'Clear information for informed conversations.'})).toBeInTheDocument();expect(await screen.findByRole('heading',{name:'Education resources are being medically reviewed'})).toBeInTheDocument();});
