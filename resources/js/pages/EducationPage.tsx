import { useQuery } from '@tanstack/react-query';import { BookOpen, ShieldCheck } from 'lucide-react';import { api } from '../api';
export default function EducationPage(){const articles=useQuery({queryKey:['education'],queryFn:async()=> (await api.get('/education/articles')).data});return <><section className="page-hero"><div className="container"><span className="eyebrow">Patient education</span><h1>Clear information for informed conversations.</h1><p>General breast-health resources with named medical review and update dates.</p></div></section><section className="section"><div className="container narrow"><div className="education-grid">{articles.data?.data?.map((a:any)=><article key={a.id}><BookOpen/><span>{a.category}</span><h2>{a.title}</h2><p>{a.summary}</p><small><ShieldCheck/> Reviewed by {a.medical_reviewer} · {new Date(a.reviewed_at).toLocaleDateString('en-NG')}</small></article>)}</div>{!articles.data?.total&&<div className="empty-state"><BookOpen/><h3>Education resources are being medically reviewed</h3><p>Nothing is published until an author, reviewer, review date and disclaimer are recorded.</p></div>}</div></section></>}
