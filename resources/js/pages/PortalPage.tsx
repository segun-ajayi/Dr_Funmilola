import { useQuery } from '@tanstack/react-query';
import { Link, useNavigate } from 'react-router-dom';
import { CalendarDays, CheckCircle2, Clock3, LogOut, MailCheck, Plus } from 'lucide-react';
import { api } from '../api';

export default function PortalPage(){const navigate=useNavigate();const me=useQuery({queryKey:['me'],queryFn:async()=> (await api.get('/me')).data.user,retry:false});const appointments=useQuery({queryKey:['my-appointments'],queryFn:async()=> (await api.get('/me/appointments')).data,retry:false,enabled:!!me.data?.email_verified});const logout=async()=>{await api.post('/auth/logout');navigate('/sign-in')};if(me.isLoading)return <div className="portal-loading">Opening your secure portal…</div>;if(me.isError)return <section className="portal-guest"><h1>Please sign in to continue.</h1><Link className="btn btn-primary" to="/sign-in">Sign in</Link></section>;return <section className="portal"><div className="container"><div className="portal-top"><div><span className="eyebrow">Patient portal</span><h1>Good to see you, {me.data.name.split(' ')[0]}.</h1></div><button onClick={logout} className="btn btn-outline-primary"><LogOut/> Sign out</button></div>{!me.data.email_verified?<div className="verify-card"><MailCheck/><div><h2>Verify your email to view appointments.</h2><p>We sent a secure verification link to <b>{me.data.email}</b>. This extra step protects your medical appointment information.</p></div></div>:<><div className="portal-actions"><div><CalendarDays/><span>Appointments</span><strong>{appointments.data?.total??0}</strong></div><div><CheckCircle2/><span>Account status</span><strong>Verified</strong></div><Link to="/book"><Plus/><span>New request</span><strong>Book care</strong></Link></div><div className="portal-section"><div><span className="eyebrow">Your care schedule</span><h2>Appointments</h2></div><Link className="btn btn-primary" to="/book">Request appointment</Link></div>{appointments.data?.data?.length?appointments.data.data.map((a:any)=><article className="appointment-row" key={a.id}><div className="appointment-date"><b>{new Date(a.starts_at).toLocaleDateString('en-NG',{day:'2-digit'})}</b><span>{new Date(a.starts_at).toLocaleDateString('en-NG',{month:'short'})}</span></div><div><h3>{a.service.name}</h3><p><Clock3/> {new Date(a.starts_at).toLocaleString('en-NG',{dateStyle:'long',timeStyle:'short'})}</p></div><span className="status-pill">{String(a.status).replaceAll('_',' ')}</span></article>):<div className="empty-state"><CalendarDays/><h3>No appointments yet</h3><p>When you request an appointment, its status and confirmed time will appear here.</p><Link className="btn btn-primary" to="/book">Book an appointment</Link></div>}</>}</div></section>}
