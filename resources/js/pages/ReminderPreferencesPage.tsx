import { useQuery } from '@tanstack/react-query';
import { Bell, CheckCircle2 } from 'lucide-react';
import { Link } from 'react-router-dom';
import { useState } from 'react';
import { api } from '../api';

export default function ReminderPreferencesPage(){const preferences=useQuery({queryKey:['reminder-preferences'],queryFn:async()=> (await api.get('/me/notification-preferences')).data.data,retry:false});const [saved,setSaved]=useState(false);if(!preferences.data)return <div className="portal-loading">Loading reminder preferences…</div>;const save=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const form=new FormData(e.currentTarget);await api.put('/me/notification-preferences',{in_app_reminders:form.has('in_app_reminders'),email_reminders:form.has('email_reminders'),push_reminders:false});setSaved(true)};return <section className="portal"><div className="container narrow"><span className="eyebrow">Patient portal</span><h1 className="settings-title">Appointment reminders</h1><p className="section-help">Choose how the practice should remind you about confirmed appointments.</p>{saved&&<div className="success-line"><CheckCircle2/> Reminder preferences saved.</div>}<form className="preference-card" onSubmit={save}><label><input type="checkbox" name="in_app_reminders" defaultChecked={preferences.data.in_app_reminders}/><Bell/><span><b>In-app reminders</b><small>See reminders in your secure patient portal.</small></span></label><label><input type="checkbox" name="email_reminders" defaultChecked={preferences.data.email_reminders}/><span><b>Email reminders</b><small>Queue reminders for your verified email address.</small></span></label><label className="disabled"><input type="checkbox" disabled/><span><b>Mobile push reminders</b><small>Available when the Android and iOS apps launch.</small></span></label><div><Link className="btn btn-outline-primary" to="/portal">Back to portal</Link><button className="btn btn-primary">Save preferences</button></div></form></div></section>}
