import { useQuery, useQueryClient } from '@tanstack/react-query';
import { ArrowLeft, CalendarDays, Plus, Trash2 } from 'lucide-react';
import { Link } from 'react-router-dom';
import { useMemo, useState } from 'react';
import { api } from '../api';

type View='day'|'week'|'month';
const iso=(date:Date)=>date.toISOString().slice(0,10);
export default function StaffCalendarPage(){const qc=useQueryClient(),[view,setView]=useState<View>('week'),[anchor,setAnchor]=useState(iso(new Date()));const range=useMemo(()=>{const center=new Date(anchor+'T12:00:00');const before=view==='day'?0:view==='week'?3:15;const after=view==='day'?0:view==='week'?3:16;const from=new Date(center),to=new Date(center);from.setDate(from.getDate()-before);to.setDate(to.getDate()+after);return {from:iso(from),to:iso(to)}},[view,anchor]);const calendar=useQuery({queryKey:['calendar',range],queryFn:async()=> (await api.get('/staff/calendar',{params:range})).data});const exceptions=useQuery({queryKey:['exceptions',range],queryFn:async()=> (await api.get('/staff/availability-exceptions',{params:range})).data.data});const add=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);await api.post('/staff/availability-exceptions',Object.fromEntries(f));e.currentTarget.reset();qc.invalidateQueries({queryKey:['exceptions']});qc.invalidateQueries({queryKey:['calendar']})};const remove=async(id:number)=>{await api.delete(`/staff/availability-exceptions/${id}`);qc.invalidateQueries({queryKey:['exceptions']})};return <section className="staff-main calendar-page"><header><div><span className="eyebrow">Practice schedule · Africa/Lagos</span><h1>Calendar</h1><p>Review appointments and manage leave, closures or additional clinics.</p></div><Link className="btn btn-outline-primary" to="/staff"><ArrowLeft/> Practice today</Link></header><div className="calendar-toolbar"><div>{(['day','week','month'] as View[]).map(v=><button className={view===v?'active':''} onClick={()=>setView(v)} key={v}>{v}</button>)}</div><input className="form-control" type="date" value={anchor} onChange={e=>setAnchor(e.target.value)}/></div><div className="calendar-layout"><section className="staff-card calendar-events"><div className="card-heading"><div><span className="eyebrow">{range.from} to {range.to}</span><h2>{view[0].toUpperCase()+view.slice(1)} schedule</h2></div></div>{calendar.data?.data?.map((event:any)=><article className="calendar-event" key={event.id}><time>{new Date(event.start).toLocaleString('en-NG',{dateStyle:'medium',timeStyle:'short'})}</time><div><h3>{event.patient.name}</h3><p>{event.title} · {event.method==='online'?'Online':'In person'}</p></div><span className={`status status-${event.status}`}>{event.status.replaceAll('_',' ')}</span></article>)}{!calendar.data?.data?.length&&<div className="staff-empty"><CalendarDays/><p>No appointments in this period.</p></div>}</section><aside className="staff-card"><div className="card-heading"><div><span className="eyebrow">Availability</span><h2>Schedule exceptions</h2></div></div><form className="exception-form" onSubmit={add}><select className="form-control" name="kind"><option value="closed">Closure or leave</option><option value="additional">Additional clinic</option></select><input className="form-control" name="label" placeholder="Reason or label" required/><label>Starts<input className="form-control" type="datetime-local" name="starts_at" required/></label><label>Ends<input className="form-control" type="datetime-local" name="ends_at" required/></label><select className="form-control" name="consultation_method"><option value="both">All consultations</option><option value="in_person">In person only</option><option value="online">Online only</option></select><button className="btn btn-primary"><Plus/> Add exception</button></form>{exceptions.data?.map((item:any)=><div className="exception-row" key={item.id}><div><b>{item.label}</b><small>{item.kind==='closed'?'Closed':'Additional clinic'} · {new Date(item.starts_at).toLocaleString('en-NG')}</small></div><button onClick={()=>remove(item.id)} aria-label={`Remove ${item.label}`}><Trash2/></button></div>)}</aside></div></section>}
