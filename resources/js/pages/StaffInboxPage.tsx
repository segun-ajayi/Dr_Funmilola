import { useQuery, useQueryClient } from '@tanstack/react-query';
import { Link } from 'react-router-dom';
import { ArrowLeft, Check, MessageCircle, Stethoscope, X } from 'lucide-react';
import { api } from '../api';
import { useState } from 'react';

export default function StaffInboxPage(){const qc=useQueryClient(),[reply,setReply]=useState<Record<number,string>>({});const inbox=useQuery({queryKey:['staff-inbox'],queryFn:async()=> (await api.get('/staff/inbox')).data,retry:false});const refresh=()=>qc.invalidateQueries({queryKey:['staff-inbox']});const send=async(id:number)=>{await api.post(`/staff/message-threads/${id}/messages`,{body:reply[id]});setReply({...reply,[id]:''});refresh()};const review=async(id:number,decision:string)=>{await api.patch(`/staff/cancellation-requests/${id}`,{decision});refresh()};if(!inbox.data)return <div className="portal-loading">Opening the practice inbox…</div>;return <section className="staff-main inbox-page"><header><div><span className="eyebrow">Practice communications</span><h1>Inbox</h1><p>Patient messages and requests that need a response.</p></div><Link className="btn btn-outline-primary" to="/staff"><ArrowLeft/> Practice today</Link></header><div className="staff-grid"><section className="staff-card"><div className="card-heading"><div><span className="eyebrow">Messages</span><h2>Patient conversations</h2></div><span className="count-badge">{inbox.data.threads.length}</span></div>{inbox.data.threads.map((t:any)=><article className="staff-thread" key={t.id}><h3>{t.subject}</h3><span>{t.patient.name}</span>{t.messages.map((m:any)=><div className="staff-message" key={m.id}><b>{m.sender.role==='patient'?m.sender.name:'Practice team'}</b><p>{m.body}</p></div>)}<div className="reply-row"><input className="form-control" placeholder="Write a reply" value={reply[t.id]||''} onChange={e=>setReply({...reply,[t.id]:e.target.value})}/><button className="btn btn-primary" disabled={!reply[t.id]} onClick={()=>send(t.id)}>Send</button></div></article>)}</section><section className="staff-card"><div className="card-heading"><div><span className="eyebrow">Review</span><h2>Cancellation requests</h2></div></div>{inbox.data.cancellations.map((c:any)=><article className="cancellation-card" key={c.id}><Stethoscope/><div><h3>{c.appointment.patient.name}</h3><p>{c.appointment.service.name}</p><small>{new Date(c.appointment.starts_at).toLocaleString('en-NG')}</small>{c.reason&&<blockquote>{c.reason}</blockquote>}</div><div className="row-actions"><button onClick={()=>review(c.id,'approved')} title="Approve"><Check/></button><button onClick={()=>review(c.id,'declined')} className="danger" title="Decline"><X/></button></div></article>)}{!inbox.data.cancellations.length&&<div className="staff-empty"><MessageCircle/><p>No cancellation requests are waiting.</p></div>}</section></div></section>}
