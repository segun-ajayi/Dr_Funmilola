import { useQuery, useQueryClient } from '@tanstack/react-query';import { ArrowLeft, Check, ExternalLink, Send, X } from 'lucide-react';import { Link } from 'react-router-dom';import { useState } from 'react';import { api } from '../api';
export default function VerificationQueuePage(){const qc=useQueryClient(),[status,setStatus]=useState('pending_review');const queue=useQuery({queryKey:['verification-queue',status],queryFn:async()=> (await api.get('/cms/verification-queue',{params:{status}})).data,retry:false});const refresh=()=>qc.invalidateQueries({queryKey:['verification-queue']});const decide=async(id:number,decision:string)=>{await api.patch(`/cms/verification-queue/${id}`,{decision});refresh()};const publish=async(id:number)=>{await api.post(`/cms/verification-queue/${id}/publish`);refresh()};if(queue.isError)return <section className="portal-guest"><h1>Power Admin access required.</h1><Link className="btn btn-primary" to="/sign-in">Sign in</Link></section>;return <section className="staff-main verification-page"><header><div><span className="eyebrow">Evidence first</span><h1>Research review</h1><p>Check each source before anything becomes public.</p></div><Link className="btn btn-outline-primary" to="/staff/cms"><ArrowLeft/> Website editor</Link></header><div className="review-tabs">{['pending_review','verified','rejected','published'].map(s=><button className={status===s?'active':''} onClick={()=>setStatus(s)} key={s}>{s.replace('_',' ')}</button>)}</div><div className="review-list">{queue.data?.data?.map((item:any)=><article key={item.id}><div><span>{item.category} · {item.confidence} confidence</span><h2>{item.claim}</h2><p>{item.source_title}</p><a href={item.source_url} target="_blank" rel="noopener noreferrer">Open authoritative source <ExternalLink/></a></div><aside>{status==='pending_review'&&<><button onClick={()=>decide(item.id,'verified')}><Check/> Mark verified</button><button className="danger" onClick={()=>decide(item.id,'rejected')}><X/> Reject</button></>}{status==='verified'&&<button onClick={()=>publish(item.id)} disabled={!item.target_type}><Send/> Publish approved record</button>}</aside></article>)}</div>{!queue.data?.data?.length&&<div className="staff-empty"><Check/><p>No records in this review state.</p></div>}</section>}
